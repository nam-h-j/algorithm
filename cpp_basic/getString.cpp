#include <bits/stdc++.h> //(1) 라이브러리 import // bits/stdc++.h는 모든 표준 라이브러리가 포함된 헤더
using namespace std;//(2) namespace 설정, 사용안하면 std::cin >> a; 이런식으로 작성해야한다.
string a; 
int main(){
    cin >> a;//(3) scanf와 같은 역할, 입력을 받음
    cout << a << "\n";//(4) printf와 같은 역할, 출력
    return 0;//(5) 종료
}